import React from 'react';
import { websiteName } from '@/app/config';

export const metadata = {
    title: websiteName + 'FAQ',
    description: '',
};

const faq = () => {

    return (
        <>


        </>
    )
}

export default faq;
